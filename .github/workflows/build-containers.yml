---
name: "Build Containers"

on:
  workflow_dispatch:
  push:
    tags:
      - '*'

jobs:
  build-containers-common:
    runs-on: ubuntu-latest
    outputs:
      containerver: ${{ steps.getcontainerver.outputs.containerver }}
    steps:
      - name: Get Container Version
        id: getcontainerver
        run: |
          echo "containerver=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"

  build-containers:
    needs: build-containers-common
    strategy:
      matrix:
        include:
          - runnertags: ubuntu-latest
            arch: amd64
          - runnertags: ubuntu-24.04-arm
            arch: arm64
    runs-on: ${{ matrix.runnertags }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ghcr
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and Push Image
        env:
          CONTAINERVER: ${{ needs.build-containers-common.outputs.containerver }}
          NEXT_PUBLIC_VERSION: ${{ github.ref_name }}
          GHCR_REPO: ghcr.io/${{ github.repository }}
        run: |
          docker buildx build --platform linux/${{ matrix.arch }} \
            -f Dockerfile.dev \
            -t ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-${{ matrix.arch }} \
            --build-arg NEXT_PUBLIC_VERSION=${{ env.NEXT_PUBLIC_VERSION }} \
            --pull \
            --no-cache \
            --provenance=false --sbom=false \
            --output "type=registry,name=${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-${{ matrix.arch }}" .

  build-container-manifest:
    needs: [build-containers, build-containers-common]
    runs-on: ubuntu-latest
    steps:
      - name: Login to ghcr
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Create Docker Manifest
        env:
          CONTAINERVER: ${{ needs.build-containers-common.outputs.containerver }}
          GHCR_REPO: ghcr.io/${{ github.repository }}
        run: |
          # Verify the architecture images
          echo "Verifying AMD64 image:"
          docker buildx imagetools inspect ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-amd64
          
          echo "Verifying ARM64 image:"
          docker buildx imagetools inspect ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-arm64
          
          # Try to remove any existing manifests first
          docker manifest rm ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }} || true
          docker manifest rm ${{ env.GHCR_REPO }}:latest || true
          
          # Create and push the version-specific manifest
          docker manifest create ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }} \
            --amend ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-amd64 \
            --amend ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-arm64

          docker manifest push ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}

          # Create and push the latest manifest
          docker manifest create ${{ env.GHCR_REPO }}:latest \
            --amend ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-amd64 \
            --amend ${{ env.GHCR_REPO }}:${{ env.CONTAINERVER }}-arm64

          docker manifest push ${{ env.GHCR_REPO }}:latest

      - name: Verify Manifest
        env:
          GHCR_REPO: ghcr.io/${{ github.repository }}
        run: |
          docker manifest inspect ${{ env.GHCR_REPO }}:latest
